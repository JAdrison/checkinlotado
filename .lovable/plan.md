

## Redesign do formulário: Light theme + headline maior

### Mudanças

1. **LeadFormDialog.tsx**:
   - Fundo do modal: de `#1A1208` (dark) para branco/creme claro (`#FFF9F0` ou similar)
   - Textos: de `text-cream` para tons escuros (`text-gray-800`, `text-gray-600`)
   - Inputs: fundo branco com borda suave, texto escuro
   - Select: mesma adaptação light
   - Headline: aumentar de `text-sm sm:text-base` para `text-lg sm:text-xl font-semibold`
   - "faturar alto" mantém destaque em ochre
   - Botão CTA: mantém estilo dourado existente
   - Close button: cor escura para contraste no fundo claro

